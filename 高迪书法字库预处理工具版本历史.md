# 高迪书法字库预处理工具 版本历史

## 2026-04-14

### Bug修复

- **修复竖向切割线拖动无效问题**（页面1-切割布局）
  - 问题：Shift+双击添加竖向切割线后，拖动移动时位置没有真正更新，横向切割线不跟随变化，切割结果仍是移动前的位置
  - 原因：`handleMouseMove` 中拖动竖线时只更新了 `state.verticalLines` 的值，但未同步更新 `stripHorizontalLines` 中对应列的 `x_start`/`x_end`
  - 修复：拖动竖线时同步更新相邻两列的边界值
  - 涉及文件：`static/js/layout.js`

### 功能改进

- **导出后自动清理中间文件**（页面4-标注出图）
  - 导出白底黑字后自动清理切割原始文件（char_XXXX.png）和缩放校正目录（scaled/），只保留最终导出目录
  - 清理旧导出目录，仅保留本次导出
  - 新增后端API：`/api/cleanup_intermediate`
  - 涉及文件：`app.py`、`static/js/annotate.js`

- **右键删除错误字**（页面4-标注出图）
  - 右键点击字符卡片弹出确认框，确认后标记为已删除（半透明+不可操作）
  - 标注时自动跳过已删除卡片，字符计数仅统计有效卡片
  - 涉及文件：`static/js/annotate.js`、`templates/annotate.html`

- **支持CJK扩展区字符**（页面4-标注出图）
  - 修复 `getUtfCode`：`charCodeAt(0)` → `codePointAt(0)`，正确显示扩展区编码（如 U+249B2）
  - 修复 `removePunctuation`：正则扩展覆盖 CJK 扩展A（U+3400-4DBF）和扩展B-F（U+20000-2FA1F），使用 `u` 标志匹配补充平面
  - 移除输入框 `maxlength="1"` 限制（扩展区字符需2个UTF-16码元），改用 `Array.from()` 提取第一个完整码点
  - 字符计数使用 `Array.from()` 替代 `.length`，正确统计包含代理对的文本
  - 文本拆分使用 `Array.from(text)` 替代 `text.split('')`
  - 涉及文件：`static/js/annotate.js`

- **图片命名规范**（页面4-标注出图）
  - BMP字符（U+0000–FFFF）：`uniXXXX` 格式，如 `uni4E00.png`
  - 扩展区字符（U+10000+）：`uXXXXX` 格式，如 `u249B2.png`
  - 符合 FontLab 字形命名规范
  - 涉及文件：`app.py`
